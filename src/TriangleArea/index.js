//Area Triangle
//el area es igual a la base por altura sobre 2

function areaTriangle(base, height) {
  let multi = base * height
  let area = multi / 2
  console.log(`Base: ${base} and Height: ${height}
  The triangle area ${area}`)
}
areaTriangle(81, 56)
